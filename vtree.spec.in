Summary: Graphically show directory tree.
Prefix: /usr
Name: vtree.git
Version: @VERSION@
Release: 1
#Group: unknown
License: GPLv2
Source: vtree-%{version}.tar.gz
BuildRoot: %{_tmppath}/%{name}-%{version}

%description
My version of tree(1), before tree(1) was in the distro.

%prep
%setup -q

%build
        ./autogen.sh
        CFLAGS="${RPM_OPT_FLAGS}" ./configure --prefix=%{prefix}
        make

%install
        rm -rf ${RPM_BUILD_ROOT}
        make prefix=${RPM_BUILD_ROOT}%{prefix} install

%clean
        [ "$RPM_BUILD_ROOT" != "/" ] && rm -rf "$RPM_BUILD_ROOT"

%files
%defattr(-,root,root)
%{_bindir}/vtree
%{_mandir}/man1/vtree.1*

%changelog
* Thu Nov 17 2011 Tommy Reynolds <reynolds@quadimodo.darkzone.un>
- Initial spec file created by autospec ver. 0.8 with rpm 3 compatibility
